// upload.js
// Upload helper placeholder.
// Actual upload is performed by `navigation.js` using the native file picker.

/**
 * Placeholder helper for uploads. Kept for API compatibility with UI hooks.
 * Does not perform any action in the demo.
 *
 * @returns {void}
 */
function uploadFile() {
  // Intentionally left blank for the focused beginner demo.
}
